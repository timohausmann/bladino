# VITE_ Frontend vars – baked in at build time.
# See Vite proxy during dev for relative path.
VITE_GRAPHQL_URL=/graphql
VITE_UPLOAD_URL=/upload

# CodeGen (Node CLI) – must be an absolute URL
CODEGEN_SCHEMA_URL=http://localhost:3001/graphql